mutation unlockOrder($id: ID!, $serving: ID!){
    updateOrder(id: $id, input: {locked: false, serving: $serving}){
        errors{
            messages
            field
        }
        order{
            gid
            locked
        }
    }
}