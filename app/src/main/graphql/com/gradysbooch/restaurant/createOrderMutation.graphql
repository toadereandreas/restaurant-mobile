mutation createOrder($color: String, $serving : String){
    createOrder(input: {color: $color, serving: $serving, locked: true, note: "_"}){
        order{
            servingId,
            color
        }
    }
}