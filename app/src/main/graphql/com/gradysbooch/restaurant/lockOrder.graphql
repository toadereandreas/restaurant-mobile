mutation lockOrder($id: ID!, , $serving: ID!){
    updateOrder(id: $id, input: {locked: true, serving: $serving}){
        errors{
            messages
            field
        }
        order{
            gid
            locked
        }
    }
}