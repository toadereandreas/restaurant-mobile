mutation lockOrder($id: ID!){
    updateOrder(id: $id, input: {locked: true}){
        errors{
            messages
            field
        }
        order{
            gid
            locked
        }
    }
}